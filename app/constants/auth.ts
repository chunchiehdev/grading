export const PUBLIC_PATHS = ['/auth/login', '/auth/register', '/auth/google', '/auth/google/callback'] as const;

export const AUTH_COOKIE_NAME = '__auth';
export const AUTH_COOKIE_MAX_AGE = 60 * 60 * 24 * 7; 
