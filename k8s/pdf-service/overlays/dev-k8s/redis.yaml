apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-dev
  namespace: grading-pdf-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-dev
  template:
    metadata:
      labels:
        app: redis-dev
    spec:
      containers:
      - name: redis-dev
        image: redis:7-alpine
        ports:
        - containerPort: 6379
        resources:
          limits:
            memory: "128Mi"
            cpu: "100m"
          requests:
            memory: "64Mi"
            cpu: "50m"
---
apiVersion: v1
kind: Service
metadata:
  name: redis-dev
  namespace: grading-pdf-dev
spec:
  selector:
    app: redis-dev
  ports:
  - port: 6379
    targetPort: 6379
  type: ClusterIP
